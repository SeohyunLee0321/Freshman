#include <iostream>
using namespace std;

int main()
{
	double a, b, c, d, e, f;
	cin >> a;
	cin >> b;
	cin >> c;
	cin >> d;
	cin >> e;
	cin >> f;

	cout << (e * d - b * f) / (a * d - b * c) << endl;

}